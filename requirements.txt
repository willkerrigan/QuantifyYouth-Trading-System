# Data & Numerical Analysis
pandas==2.2.0
numpy==1.24.3
scipy==1.11.2

# Backtesting & Trading
# backtrader and ccxt were pinned here but imported nowhere in the codebase, and
# backtrader==1.9.94.1 has no files on PyPI, so `pip install -r requirements.txt`
# failed outright in a clean environment. Removed rather than re-pinned; add back
# with a resolvable version if either is actually adopted.
# alpaca-py 0.20.0 capped websockets<12 while yfinance 0.2.65 requires
# websockets>=13, making the pair uninstallable (CI failed on ResolutionImpossible).
# 0.44.0 lifts the cap; the APIs used here (TradingClient, StockHistoricalDataClient,
# MarketOrderRequest, OrderSide, TimeInForce) are unchanged.
alpaca-py==0.44.0
yfinance==0.2.65

# Configuration
pyyaml==6.0.1
python-dotenv==1.0.0

# Utilities
click==8.1.7
tqdm==4.66.1
loguru==0.7.2

# Testing
pytest==7.4.3
pytest-cov==4.1.0
